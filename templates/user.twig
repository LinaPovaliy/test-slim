<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <title>Пользователь {{ user.name }}</title>
    <style>
        .user-card {
            max-width: 18rem;
            margin: auto;
            text-align: center;
        }

        .user-image {
            max-width: 10rem;
            max-height: 10rem;
        }
    </style>
</head>
<body>
<div class="container mt-3">
    <div class="card user-card">
        <div class="center-image">
            <img src="{{ user.image }}" class="card-img-top user-image" alt="{{ user.name }}">
        </div>
        <div class="card-body">
            <h5 class="card-title">{{ user.name }}</h5>
            <p class="card-text">E-mail: <a href="mailto:{{ user.email }}">{{ user.email }}</a></p>
            <a href="/users" class="btn btn-primary">Список пользователей</a>
        </div>
    </div>
</div>
</body>
</html>